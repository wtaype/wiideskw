// pin.rs — Gestión de seguridad local y configuración de PIN (Placeholders por el momento)

pub fn configurar(_pin: String) -> Result<(), String> {
    Ok(())
}

pub fn verificar(_pin: String) -> Result<bool, String> {
    Ok(true)
}
